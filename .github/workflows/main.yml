name: Build McInvest

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ดึงโค้ด
        uses: actions/checkout@v4

      - name: ติดตั้ง JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Build
        run: mvn -B clean package

      - name: อัปโหลดไฟล์ jar
        uses: actions/upload-artifact@v4
        with:
          name: McInvest-jar
          path: target/*.jar
          if-no-files-found: error
